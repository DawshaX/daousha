# حالة نطاق XDAW NOVA المجاني — 17 أغسطس 2026

أظهر الاستعلام المباشر للمنطقة `xdawnova.int.eu.org` في Cloudflare أنها ما زالت بحالة **pending**، ولم تُفعّل بعد (`activated_on: null`). أسماء الخوادم المسجلة في Cloudflare هي `adele.ns.cloudflare.com` و`vicente.ns.cloudflare.com`، والمنطقة ليست متوقفة.

لا توجد خطوة ربط بالموقع أو تعديل DNS آمنة قبل انتقال الحالة إلى **active**. لم يُنشأ أو يُعدل أي سجل DNS في هذا الفحص.
