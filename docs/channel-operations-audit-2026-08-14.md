# تدقيق التشغيل وربط القنوات — 2026-08-14

## YouTube

مهمة النشر الدوري `xdaw-youtube-reel02-ar-60002` مفعلة على فاصل عشر دقائق، ومعرّفها `cExLbnJCzgGR8xvSQjeNzd`. سجّل تاريخ التنفيذ الفعلي نشر نسخة اختبار خاصة ثم نشرًا عامًا واحدًا للمشروع `60002` بعد اجتياز الحواجز؛ رابط الفيديو العام هو `https://www.youtube.com/watch?v=cG59EOkUgcY`.

> **نتيجة الحراسة:** لم ينتج عن الجدول 88 فيديو عامًا. تُظهر الدورات التالية استجابة `skipped: inactive`، ما يؤكد أن الجدولة لا تتجاوز حالة المشروع أو الحواجز لتكرار النشر.

## Telegram ومراقبة النطاق

مهمة مراقبة EU.org `xdaw-euorg-domain-monitor` مفعلة كل ست ساعات ومعرّفها `koiTi5Bcps9sEvsnNqnrX5`. كان آخر تشغيل ناجحًا في `2026-08-14T06:11:59Z` وأعاد حالة `pending` مع `notified: true`؛ ما يعني أن Telegram تلقى إشعارًا بالحالة دون أن ينفذ أي تغيير على DNS أو Facebook OAuth.

| المسار | الحالة الحالية | أثرها على التشغيل المستقل |
|---|---|---|
| YouTube OAuth + Heartbeat | مفوّض ومفعّل بحواجز | نشر مشروع واحد عند اكتمال المراجعة والحدود فقط |
| Telegram | مفوّض للتنبيهات | يستقبل حالة الدورات والأخطاء دون إدارة حساب اجتماعي |
| Instagram `@xdaw_nova` | الموصل مفوّض والحساب صحيح | يُنشأ كل Reel كبطاقة تأكيد منفصلة قبل النشر العام |
| نطاق EU.org | قيد المراجعة الخارجية | يحجب Facebook OAuth الدائم إلى حين تفويض DNS |

## استنتاج تشغيلي

لا يُرفع سقف النشر لمجرد أن الماكينة تعمل؛ دور Heartbeat هو تنفيذ القرارات المؤهلة فقط. تبقى كل منصة غير مفوضة إنتاجيًا خارج طابور التوزيع، ويظل TikTok Sandbox منفصلًا عن الإنتاج إلى أن توافق TikTok على التطبيق ويُمنح تفويض إنتاج مستقل.

## Instagram

فحص القراءة فقط للموصل المفوض في `2026-08-14` أعاد الحساب `@xdaw_nova` بالاسم `xDaw NoVa` والسيرة الثنائية المعتمدة ورابط YouTube، ولم يجد أي منشورات سابقة. الموصل مفعّل ويملك حسابًا نشطًا واحدًا. لا يملك المحرك مسار نشر صامت في Instagram: أي Reel يحتاج إنشاء بطاقة تأكيد منفصلة للمستخدم قبل التنفيذ العام.

## Facebook

في `2026-08-14` جرى فتح Meta Graph API Explorer على تطبيق **XDAW NOVA Publisher** واختيار **رمز وصول الصفحة: XDAW NOVA** بنجاح، مع الصلاحيات الظاهرة `pages_show_list` و`business_management` و`pages_read_engagement` و`pages_manage_metadata` و`pages_manage_posts`. لم يُنسخ الرمز إلى التوثيق أو الدردشة. يبقى تجديد السر المحمي داخل المنصة والتحقق من `id,name` خطوة منفصلة، بينما يبقى Facebook OAuth الدائم معلقًا على تفويض `xdawnova.int.eu.org` في DNS العام.

أُدخل رمز الصفحة الجديد في مخزن أسرار المنصة الآمن، ونجح اختبار Graph API المحدود في إرجاع `id: 1265727539958933` و`name: XDAW NOVA`. أصبح المسار المؤقت للصفحة جاهزًا للقراءة أو النشر بعد تأكيد صريح لكل عملية، لكنه لا يزيل عائق OAuth الدائم المرتبط بموافقة EU.org أو يبرر إدراج Facebook في توزيع دوري صامت.

## مصفوفة التوزيع الموحد

| القناة | حالة التفويض الفعلية | موضعها في التوزيع الآن | القيد الحاكم |
|---|---|---|---|
| YouTube | OAuth مفوّض؛ دورة نشر نشطة | نشر دوري محكوم للمشاريع المؤهلة | معاينة خاصة، الحقوق، السلامة، سقف النشر، والفاصل الزمني |
| TikTok | Production مهيأ فقط؛ Sandbox اجتاز OAuth وDraft | مستبعد من الإنتاج | موافقة TikTok ثم OAuth إنتاج مستقل؛ لا تستخدم رموز Sandbox |
| Instagram | الموصل مفوّض لـ`@xdaw_nova` | بطاقة تأكيد لكل Reel | لا نشر صامت أو جدولة مباشرة |
| Facebook | رمز صفحة جديد متحقق | تأكيد صريح لكل نشر | OAuth الدائم ينتظر تفويض EU.org؛ لا توزيع دوري صامت |
| Telegram | مفوّض للتنبيهات | إشعارات تشغيلية فقط | ليس وجهة نشر فيديو |

تؤكد قراءة سياسة النشر الحالية أن الوضع `guarded_auto` مفعّل، لكن **النشر العام معطّل** ومفتاح الإيقاف الفوري مفعّل، مع اشتراط نسخة اختبار خاصة وفاصل أدنى قدره 10 دقائق. لذلك لا يمكن لأي جدول أن يتحول إلى منفذ نشر شامل لجميع القنوات تلقائيًا لمجرد اكتمال إعداداته.

## نتيجة الاختبارات

اجتازت المجموعة الكاملة في `2026-08-14` عدد **36 اختبارًا عبر 25 ملفًا**، بما فيها حواجز النشر، وجهات التوزيع، Facebook Page Token، TikTok Sandbox، وTelegram. لا يُعد هذا بديلًا عن التفويض الخارجي للإنتاج في TikTok أو موافقة النطاق لـFacebook؛ بل يثبت أن المنصة تمنع إدخالهما في المسار العام قبل الجاهزية.
