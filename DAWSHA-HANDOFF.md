# XDAW NOVA // DAWSHA — حالة التسليم والتطوير

> **آخر تحديث:** 17 أغسطس 2026. هذه الوثيقة تعطي أي مطوّر أو وكيل آخر السياق الكامل لمتابعة المنصة من دون كشف كلمات المرور أو مفاتيح API أو رموز القنوات.

## هوية المنتج

المنتج هو **XDAW NOVA / DAWSHA Content Engine**: غرفة تحكم ثنائية اللغة لإعداد ومراجعة وجدولة محتوى فيديو أصلي أو مرخّص، بهوية دارك نيون حمراء. التطبيق الأساسي هو مصدر الحقيقة؛ Telegram مرآة تحكم لنفس المنفذ ونفس سجل التدقيق.

## المكونات التي تعمل حاليًا

| المجال | الحالة | نقاط التنفيذ الأساسية |
|---|---|---|
| الواجهة | تعمل | React 19 + TypeScript + Tailwind 4؛ لوحة `NOVAConsole.tsx` بهوية DAWSHA. |
| الخادم | يعمل | Express + tRPC؛ مساراته في `server/routers.ts`. |
| قاعدة البيانات | تعمل | MySQL/TiDB + Drizzle؛ المخطط في `drizzle/schema.ts` والمigrations المسجلة حتى 0019. |
| NOVA Assistant | يعمل | `server/novaAssistant.ts`؛ أوامر حتمية للويب وTelegram، ذاكرة مقيدة، وحجب أسرار قبل التحليل. |
| Gemini وOpenAI | يعملان لمسودات صريحة فقط | لا صلاحية لهما للنشر أو الجدولة أو تعديل السياسات. |
| Perplexity API | متوقف عمدًا | لا يوجد استخدام مدفوع؛ البحث البديل يدوي موثق. |
| Pinterest | إلهام فقط | ممنوع تنزيل أو استخدام أو إعادة توزيع محتوى Pins. |
| TikTok | معطل عمدًا | لا نشر حتى الموافقة الرسمية. |

## قواعد لا يجوز تجاوزها

1. لا تنسخ فيديوهات أو صور الغير، ولا تزيل علامات مائية، ولا تحول الإلهام من Pinterest إلى أصل إنتاج.
2. لا تحفظ أو ترسل كلمات مرور، رموز تحقق، أو مفاتيح API إلى السجل أو المحللات أو Git.
3. النشر العام يمر دائمًا عبر: **دليل الحقوق → السلامة → إقرار المعاينة → Canary خاص → فحص سياسة النشر → الإرسال**.
4. السياسة الفعالة: `guarded_auto`، و`requirePrivateCanary=true`، والفاصل الأدنى بين المنشورات `10` دقائق، وسقف اليوم `6` منشورات عامة.
5. إذا كانت صحة قناة ما غير سليمة، لا تتجاوزها؛ سجّل السبب وأكمل القنوات السليمة فقط عند السماح.

## حالة القنوات والإنتاج عند التسليم

| العنصر | الوضع عند آخر تسليم |
|---|---|
| YouTube | مفوض وسليم؛ نُشرت نسخ عامّة عبر المسار المحكوم. |
| Instagram | مفوض وسليم، لكن لم ينفذ إرسال جديد في آخر دورة. |
| Facebook | مفوض لكن مراقب الصحة كان ينتظر/يعالج أول فحص؛ لا إرسال جديد قبل نتيجة صحة سليمة. |
| Telegram | مرآة تحكم مربوطة بمنفذ NOVA. |
| منشورات YouTube الأخيرة | English: `https://www.youtube.com/watch?v=NB1G3w4xHaI`، عربي: `https://www.youtube.com/watch?v=sM5udxuq5z4`. |
| النسخ المتبقية | لا تنتج أو تنشر إلا عند توفر فيديوهات أصلية أو مرخّصة موثقة؛ لا توجد رخصة لاستخدام Pinterest كمصدر فيديو. |

## التشغيل في بيئة جديدة

1. استنسخ المستودع ثم نفّذ `pnpm install`.
2. راجع `docs/ENVIRONMENT_TEMPLATE.md` وأنشئ متغيرات البيئة عبر مدير أسرار البيئة الجديدة فقط.
3. أنشئ قاعدة بيانات MySQL/TiDB، واضبط `DATABASE_URL`.
4. طبّق migrations Drizzle بالترتيب (`drizzle/migrations`) قبل تشغيل الخادم.
5. شغّل `pnpm run dev` للتطوير و`pnpm test -- --run` للتحقق.
6. أعد توثيق OAuth للقنوات في البيئة الجديدة؛ رموز الوصول السابقة لا تُنسخ في Git ولا ينبغي افتراض استمرارها بعد النقل.

## الاختبارات وحالة الجودة

آخر تشغيل محلي مرّ بـ **156 اختبارًا ناجحًا** و**7 اختبارات حية متخطاة عمدًا** لكونها تعتمد على قنوات أو خدمات خارجية. تغطي الاختبارات الحواجز، النشر، Telegram، الحقوق، والواجهة.

## ملفات البداية للمطوّر القادم

| الملف | لماذا يبدأ به المطوّر |
|---|---|
| `DAWSHA-TRANSFER-GUIDE.md` | دليل بنية المشروع والنقل. |
| `docs/ENVIRONMENT_TEMPLATE.md` | أسماء متغيرات البيئة المطلوبة بدون أي قيمة حساسة. |
| `todo.md` | سجل المميزات المنجزة والمهام المتبقية. |
| `server/novaAssistant.ts` | سلوك الوكيل الموحد وحواجزه. |
| `server/telegramCommands.ts` | تكامل Telegram. |
| `docs/` | حدود الحقوق ومسار الإنتاج والبحث المرخص. |

## طريقة تحديث GitHub

استخدم فرع `main` للمستودع `https://github.com/DawshaX/daousha`. قبل كل دفع:

```bash
git status
pnpm test -- --run
git add <files>
git commit -m "وصف واضح للتغيير"
git push origin main
```

لا تضف أبدًا `.env`، مجلد `secrets/`، رموز الجلسات، ملفات الوسائط الكبيرة غير المرخصة، أو `node_modules`.
