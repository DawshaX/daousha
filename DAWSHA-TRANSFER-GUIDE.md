# XDAW NOVA // DAWSHA — دليل النقل والتطوير المستقل

هذا المستند يمثل المرجع الشامل لنقل وتطوير منصة **XDAW NOVA (دوشة)** في أي بيئة محلية أو سحابية أخرى دون فقدان أي سياق أو حواجز أو بنية.

---

## 1. نظرة عامة على الهندسة التقنية
- **الواجهة:** React 19 + TypeScript + Tailwind CSS 4 (مع هوية DAWSHA الداكنة النيونية).
- **الخادم:** Node.js + Express + tRPC 11 (موصلات آمنة ومسارات محكومة).
- **قاعدة البيانات:** MySQL / TiDB باستخدام Drizzle ORM (المرحلة الحالية: 19 Migration مسجلة).
- **المساعد الذكي:** محرك NOVA الحتمي الموحد للويب وTelegram (مع 22 أداة حتمية، وحجب تلقائي للأسرار، ومسودات إرشادية لـ Gemini وOpenAI).

---

## 2. هيكل المجلدات والملفات الأساسية
- `client/src/components/NOVAConsole.tsx`: لوحة تحكم DAWSHA النيونية والذكاء الاصطناعي والمؤشرات الحية.
- `server/novaAssistant.ts`: نواة مساعد NOVA ومنفذ الويب/Telegram المشترك وحواجز الأسرار والتحليل الآمن.
- `server/routers.ts`: مسارات tRPC وقواعد الحقوق والسلامة والنشر ومتابعة القنوات.
- `drizzle/schema.ts`: مخطط قاعدة البيانات الشامل (المشروعات، المواد، النشر، السياسات).
- `docs/`: التوثيق المعماري وقواعد الحقوق ومسارات النشر المرخصة.

---

## 3. حواجز الأمان والسلامة (Core Guardrails)
1. **لا تنزيل أو سرقة محتوى الغير:** المناهج تعتمد على الأصول الأصلية أو المرخّصة (Pexels / Pixabay / Mixkit) مع حظر قاطع لتنزيل فيديوهات Pinterest أو إزالة العلامات المائية.
2. **حجب الأسرار التلقائي:** أي كلمة مرور، رمز تحقق، أو مفتاح وصول يُحجب فورًا قبل أن يصل إلى أي محلل أو خادم خارجي، ويُسجل السجل بصورة منقحة فقط.
3. **Canary وموافقة البشر:** لا يُنشر أي فيديو عامًا إلا بعد إقرار معاينة بشري، وفحص حواجز حقوق/سلامة، ومرور نسخة خاصة (Canary)، والتزام بفاصل زمني إلزامي (10 دقائق) بين المنشورات.
4. **تكامل Telegram:** واجهة تحكم ثانية مرتبطة بنفس منفذ الويب تمامًا وبنفس السجل وقواعد الحماية.

---

## 4. خطوات التشغيل المحلي في أي بيئة جديدة
1. استنساخ المستودع:
   ```bash
   git clone https://github.com/DawshaX/daousha.git
   cd daousha
   ```
2. تثبيت الاعتماديات:
   ```bash
   pnpm install
   ```
3. إعداد متغيرات البيئة (`.env`):
   ```env
   DATABASE_URL=mysql://...
   JWT_SECRET=...
   GEMINI_API_KEY=...
   OPENAI_API_KEY=...
   ```
4. تشغيل خادم التطوير:
   ```bash
   pnpm run dev
   ```

---
*تم إعداد هذا الدليل بواسطة محرك XDAW NOVA // DAWSHA لضمان استمرارية التطوير بلا توقف.*
